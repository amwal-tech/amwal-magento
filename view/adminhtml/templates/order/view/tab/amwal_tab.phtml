<?php
/**
 * Amwal Order Summary Tab — Admin Template
 *
 * @var \Amwal\Payments\Block\Adminhtml\Order\View\Tab\AmwalTab $block
 */

$amwalData = $block->getDecodedAmwalData();
$orderData = $block->getOrderData();
$currentOrder = $block->getCurrentOrder();
$storeStatus = $currentOrder ? $currentOrder->getStatus() : '';

// SVG helper for inline icons
$svgChevron = '';
?>

<?php if (empty($amwalData)): ?>
    <!-- No Data State -->
    <div class="amwal-no-data">
        <svg class="amwal-no-data__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M9 12h6m-3-3v6m-7 4h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
        </svg>
        <div class="amwal-no-data__title"><?= $block->escapeHtml(__('No Amwal Order Data')) ?></div>
        <div class="amwal-no-data__text"><?= $block->escapeHtml(__('No Amwal payment data is available for this order.')) ?></div>
    </div>
<?php else: ?>
<div class="amwal-tab-container">

    <!-- ========== HEADER BAR ========== -->
    <div class="amwal-tab-header">
        <div class="amwal-tab-header__logo">
            <img class="amwal-logo" alt="amwal" src="<?= $block->escapeUrl($block->getViewFileUrl('Amwal_Payments::images/amwal.png')) ?>"/>
        </div>

        <div class="amwal-tab-header__actions">
            <?php $type = $amwalData['type'] ?? ''; ?>
            <?php if ($type): ?>
                <span class="amwal-type-pill <?= $block->escapeHtmlAttr($block->getTypeBadgeClass($type)) ?>">
                    <?= $block->escapeHtml($type) ?>
                </span>
            <?php endif; ?>

            <?php $status = $amwalData['status'] ?? ''; ?>
            <span class="amwal-badge <?= $block->escapeHtmlAttr($block->getStatusBadgeClass($status)) ?>">
                <?= $block->escapeHtml($status ?: (string)__('N/A')) ?>
            </span>

            <?php if ($block->getAmwalOrderUrl()): ?>
                <a href="<?= $block->escapeUrl($block->getAmwalOrderUrl()) ?>"
                   target="_blank" class="amwal-btn amwal-btn--outline amwal-btn--sm">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6"/>
                        <polyline points="15 3 21 3 21 9"/>
                        <line x1="10" y1="14" x2="21" y2="3"/>
                    </svg>
                    <?= $block->escapeHtml(__('View in Amwal')) ?>
                </a>
            <?php endif; ?>

            <?php if ($block->shouldShowUpdateButton()): ?>
                <button type="button" class="amwal-btn amwal-btn--primary amwal-btn--sm amwal-order-status-update">
                    <?= $block->escapeHtml(__('Update Order Status')) ?>
                </button>
            <?php endif; ?>
        </div>
    </div>

    <!-- ========== SECTION: Transaction Overview ========== -->
    <div class="amwal-section" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="2" y="3" width="20" height="18" rx="2"/>
                    <line x1="2" y1="9" x2="22" y2="9"/>
                </svg>
                <?= $block->escapeHtml(__('Transaction Overview')) ?>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <div class="amwal-kv-grid">
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Amwal Order ID')) ?></span>
                    <span class="amwal-kv-value amwal-kv-value--mono"><?= $block->escapeHtml($amwalData['id'] ?? (string)__('N/A')) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Reference ID')) ?></span>
                    <span class="amwal-kv-value amwal-kv-value--mono"><?= $block->escapeHtml($amwalData['ref_id'] ?? (string)__('N/A')) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Magento Order ID')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['order_details']['order_id'] ?? (string)__('N/A')) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Status')) ?></span>
                    <span class="amwal-kv-value">
                        <span class="amwal-badge <?= $block->escapeHtmlAttr($block->getStatusBadgeClass($amwalData['status'] ?? '')) ?>">
                            <?= $block->escapeHtml($amwalData['status'] ?? (string)__('N/A')) ?>
                        </span>
                        <?php if (!empty($amwalData['failure_reason'])): ?>
                            <div class="amwal-failure-reason"><?= $block->escapeHtml($amwalData['failure_reason']) ?></div>
                        <?php endif; ?>
                    </span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Transaction Type')) ?></span>
                    <span class="amwal-kv-value">
                        <span class="amwal-type-pill <?= $block->escapeHtmlAttr($block->getTypeBadgeClass($amwalData['type'] ?? '')) ?>">
                            <?= $block->escapeHtml($amwalData['type'] ?? (string)__('N/A')) ?>
                        </span>
                    </span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Created At')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatDate($amwalData['created_at'] ?? null)) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Webhook Processed')) ?></span>
                    <span class="amwal-kv-value">
                        <?php $webhookProcessed = !empty($orderData['amwal_webhook_processed']); ?>
                        <span class="amwal-badge <?= $webhookProcessed ? 'amwal-badge--success' : 'amwal-badge--warning' ?>">
                            <?= $block->escapeHtml($webhookProcessed ? (string)__('Yes') : (string)__('No')) ?>
                        </span>
                    </span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Store Order Status')) ?></span>
                    <span class="amwal-kv-value">
                        <span class="amwal-badge <?= $block->escapeHtmlAttr($block->getStatusBadgeClass($storeStatus)) ?>">
                            <?= $block->escapeHtml($storeStatus ?: (string)__('N/A')) ?>
                        </span>
                    </span>
                </div>
            </div>
        </div>
    </div>

    <!-- ========== SECTION: Payment Details ========== -->
    <div class="amwal-section" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="1" y="4" width="22" height="16" rx="2"/>
                    <line x1="1" y1="10" x2="23" y2="10"/>
                </svg>
                <?= $block->escapeHtml(__('Payment Details')) ?>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <?php $cardDisplay = $block->getMaskedCardDisplay(); ?>
            <?php if ($cardDisplay): ?>
                <div style="margin-bottom: 16px;">
                    <span class="amwal-kv-label" style="display:block; margin-bottom:6px;"><?= $block->escapeHtml(__('Card')) ?></span>
                    <div class="amwal-card-display">
                        <?php
                        $maskedParts = explode(' — ', $cardDisplay);
                        echo $block->escapeHtml($maskedParts[0] ?? '');
                        if (!empty($maskedParts[1])): ?>
                            <span class="amwal-card-display__brand"><?= $block->escapeHtml($maskedParts[1]) ?></span>
                        <?php endif; ?>
                    </div>
                </div>
            <?php endif; ?>

            <div class="amwal-kv-grid">
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Payment Method')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['payment_method'] ?? (string)__('N/A')) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Payment Option')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['payment_option'] ?? (string)__('N/A')) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Payment Brand')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['paymentBrand'] ?? (string)__('N/A')) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Funding Method')) ?></span>
                    <span class="amwal-kv-value" style="text-transform: capitalize;"><?= $block->escapeHtml($amwalData['funding_method'] ?? (string)__('N/A')) ?></span>
                </div>
                <?php if (!empty($amwalData['issuer'])): ?>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Issuer')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['issuer']) ?></span>
                </div>
                <?php endif; ?>
                <?php if (!empty($amwalData['card_holder'])): ?>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Card Holder')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['card_holder']) ?></span>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>

    <!-- ========== SECTION: Financial Summary ========== -->
    <div class="amwal-section" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <line x1="12" y1="1" x2="12" y2="23"/>
                    <path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/>
                </svg>
                <?= $block->escapeHtml(__('Financial Summary')) ?>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <div class="amwal-financial-summary">
                <div class="amwal-financial-row">
                    <span class="amwal-financial-row__label"><?= $block->escapeHtml(__('Subtotal')) ?></span>
                    <span class="amwal-financial-row__value"><?= $block->escapeHtml($block->formatCurrency($amwalData['amount'] ?? 0)) ?></span>
                </div>

                <?php $discount = (float)($amwalData['discount'] ?? 0); ?>
                <?php if ($discount != 0): ?>
                <div class="amwal-financial-row amwal-financial-row--discount">
                    <span class="amwal-financial-row__label"><?= $block->escapeHtml(__('Discount')) ?></span>
                    <span class="amwal-financial-row__value">-<?= $block->escapeHtml($block->formatCurrency($discount)) ?></span>
                </div>
                <?php endif; ?>

                <div class="amwal-financial-row">
                    <span class="amwal-financial-row__label"><?= $block->escapeHtml(__('Shipping')) ?></span>
                    <span class="amwal-financial-row__value"><?= $block->escapeHtml($block->formatCurrency($amwalData['shipping'] ?? 0)) ?></span>
                </div>

                <div class="amwal-financial-row">
                    <span class="amwal-financial-row__label"><?= $block->escapeHtml(__('Taxes')) ?></span>
                    <span class="amwal-financial-row__value"><?= $block->escapeHtml($block->formatCurrency($amwalData['taxes'] ?? 0)) ?></span>
                </div>

                <?php $fees = (float)($amwalData['fees'] ?? 0); ?>
                <?php if ($fees != 0): ?>
                <div class="amwal-financial-row">
                    <span class="amwal-financial-row__label"><?= $block->escapeHtml(__('Fees')) ?></span>
                    <span class="amwal-financial-row__value"><?= $block->escapeHtml($block->formatCurrency($fees)) ?></span>
                </div>
                <?php endif; ?>

                <?php $convenienceFee = (float)($amwalData['convenience_fee'] ?? 0); ?>
                <?php if ($convenienceFee != 0): ?>
                <div class="amwal-financial-row">
                    <span class="amwal-financial-row__label"><?= $block->escapeHtml(__('Convenience Fee')) ?></span>
                    <span class="amwal-financial-row__value"><?= $block->escapeHtml($block->formatCurrency($convenienceFee)) ?></span>
                </div>
                <?php endif; ?>

                <hr class="amwal-financial-divider">

                <div class="amwal-financial-total">
                    <span><?= $block->escapeHtml(__('Total')) ?></span>
                    <span class="amwal-financial-total__value"><?= $block->escapeHtml($block->formatCurrency($amwalData['total_amount'] ?? 0)) ?></span>
                </div>

                <?php if (isset($amwalData['payment_amount']) && $amwalData['payment_amount'] != ($amwalData['total_amount'] ?? 0)): ?>
                <div class="amwal-financial-row" style="margin-top: 8px; border-top: 1px dashed var(--amwal-border); padding-top: 12px;">
                    <span class="amwal-financial-row__label"><?= $block->escapeHtml(__('Payment Amount')) ?></span>
                    <span class="amwal-financial-row__value" style="font-weight: 600;"><?= $block->escapeHtml($block->formatCurrency($amwalData['payment_amount'])) ?></span>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>

    <!-- ========== SECTION: Amwal Fees (collapsed by default) ========== -->
    <div class="amwal-section amwal-section--collapsed" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <circle cx="12" cy="12" r="10"/>
                    <path d="M9.09 9a3 3 0 015.83 1c0 2-3 3-3 3"/>
                    <line x1="12" y1="17" x2="12.01" y2="17"/>
                </svg>
                <?= $block->escapeHtml(__('Amwal Fees & Payout')) ?>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <div class="amwal-kv-grid">
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Amwal Fee')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatCurrency($amwalData['amwal_fee'] ?? 0)) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Amwal Tax / VAT')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatCurrency($amwalData['amwal_tax_vat'] ?? 0)) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Brand Percentage')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml(($amwalData['brand_percentage'] ?? '0') . '%') ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Brand Static Fee')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatCurrency($amwalData['brand_static_fee'] ?? 0)) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Merchant Payout')) ?></span>
                    <span class="amwal-kv-value" style="font-weight: 700; color: var(--amwal-brand-dark);">
                        <?= $block->escapeHtml($block->formatCurrency($amwalData['merchant_payout'] ?? 0)) ?>
                    </span>
                </div>
            </div>
        </div>
    </div>

    <!-- ========== SECTION: Installment (conditional) ========== -->
    <?php if ($block->hasInstallment()): ?>
    <div class="amwal-section" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
                    <line x1="16" y1="2" x2="16" y2="6"/>
                    <line x1="8" y1="2" x2="8" y2="6"/>
                    <line x1="3" y1="10" x2="21" y2="10"/>
                </svg>
                <?= $block->escapeHtml(__('Installment Details')) ?>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <div class="amwal-kv-grid">
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Duration')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['installment_duration'] . ' ' . (string)__('Months')) ?></span>
                </div>
                <?php if (!empty($amwalData['installment_status'])): ?>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Installment Status')) ?></span>
                    <span class="amwal-kv-value">
                        <span class="amwal-badge <?= $block->escapeHtmlAttr($block->getStatusBadgeClass($amwalData['installment_status'])) ?>">
                            <?= $block->escapeHtml($amwalData['installment_status']) ?>
                        </span>
                    </span>
                </div>
                <?php endif; ?>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Installment Fee')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatCurrency($amwalData['installment_fee'] ?? 0)) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Installment Fee VAT')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatCurrency($amwalData['installment_fee_vat'] ?? 0)) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Murabha Fee')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatCurrency($amwalData['murabha_fee'] ?? 0)) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Murabha Fee VAT')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatCurrency($amwalData['murabha_fee_vat'] ?? 0)) ?></span>
                </div>
            </div>
        </div>
    </div>
    <?php endif; ?>

    <!-- ========== SECTION: Customer Information ========== -->
    <div class="amwal-section" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/>
                    <circle cx="12" cy="7" r="4"/>
                </svg>
                <?= $block->escapeHtml(__('Customer Information')) ?>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <div class="amwal-kv-grid">
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Name')) ?></span>
                    <span class="amwal-kv-value">
                        <?= $block->escapeHtml(trim(($amwalData['client_first_name'] ?? '') . ' ' . ($amwalData['client_last_name'] ?? '')) ?: (string)__('N/A')) ?>
                    </span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Email')) ?></span>
                    <span class="amwal-kv-value">
                        <?php if (!empty($amwalData['client_email'])): ?>
                            <a href="mailto:<?= $block->escapeHtmlAttr($amwalData['client_email']) ?>"><?= $block->escapeHtml($amwalData['client_email']) ?></a>
                        <?php else: ?>
                            <?= $block->escapeHtml((string)__('N/A')) ?>
                        <?php endif; ?>
                    </span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Phone')) ?></span>
                    <span class="amwal-kv-value">
                        <?php if (!empty($amwalData['client_phone_number'])): ?>
                            <a href="tel:<?= $block->escapeHtmlAttr($amwalData['client_phone_number']) ?>"><?= $block->escapeHtml($amwalData['client_phone_number']) ?></a>
                        <?php else: ?>
                            <?= $block->escapeHtml((string)__('N/A')) ?>
                        <?php endif; ?>
                    </span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Registered Customer')) ?></span>
                    <span class="amwal-kv-value">
                        <?php $isRegistered = !empty($amwalData['client_registered']); ?>
                        <span class="amwal-badge <?= $isRegistered ? 'amwal-badge--success' : 'amwal-badge--default' ?>">
                            <?= $block->escapeHtml($isRegistered ? (string)__('Yes') : (string)__('No')) ?>
                        </span>
                        <?php if (!empty($amwalData['has_new_registration'])): ?>
                            <span class="amwal-badge amwal-badge--info" style="margin-left: 6px;"><?= $block->escapeHtml((string)__('New Registration')) ?></span>
                        <?php endif; ?>
                    </span>
                </div>
            </div>
        </div>
    </div>

    <!-- ========== SECTION: Shipping & Address ========== -->
    <div class="amwal-section" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/>
                    <circle cx="12" cy="10" r="3"/>
                </svg>
                <?= $block->escapeHtml(__('Shipping & Address')) ?>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <div class="amwal-two-col">
                <div>
                    <div class="amwal-two-col__block-title"><?= $block->escapeHtml(__('Shipping Method')) ?></div>
                    <?php $shipping = $amwalData['shipping_details'] ?? []; ?>
                    <?php if (!empty($shipping)): ?>
                        <div class="amwal-kv-grid" style="border: 1px solid #f3f4f6; border-radius: 6px; overflow: hidden;">
                            <div class="amwal-kv-item">
                                <span class="amwal-kv-label"><?= $block->escapeHtml(__('Method')) ?></span>
                                <span class="amwal-kv-value"><?= $block->escapeHtml($shipping['label'] ?? (string)__('N/A')) ?></span>
                            </div>
                            <div class="amwal-kv-item">
                                <span class="amwal-kv-label"><?= $block->escapeHtml(__('Price')) ?></span>
                                <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatCurrency($shipping['price'] ?? 0)) ?></span>
                            </div>
                        </div>
                    <?php else: ?>
                        <span style="color: var(--amwal-muted); font-size: 14px;"><?= $block->escapeHtml((string)__('N/A')) ?></span>
                    <?php endif; ?>
                </div>
                <div>
                    <div class="amwal-two-col__block-title"><?= $block->escapeHtml(__('Delivery Address')) ?></div>
                    <div class="amwal-address-block"><?= $block->escapeHtml($block->getFormattedAddress()) ?></div>
                </div>
            </div>
        </div>
    </div>

    <!-- ========== SECTION: Order Items ========== -->
    <?php $orderItems = $block->getOrderItems(); ?>
    <?php if (!empty($orderItems)): ?>
    <div class="amwal-section" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/>
                    <line x1="3" y1="6" x2="21" y2="6"/>
                    <path d="M16 10a4 4 0 01-8 0"/>
                </svg>
                <?= $block->escapeHtml(__('Order Items')) ?>
                <span class="amwal-badge amwal-badge--default" style="font-size: 11px;"><?= count($orderItems) ?></span>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content" style="padding: 0;">
            <table class="amwal-items-table">
                <thead>
                    <tr>
                        <th style="width: 60px;"></th>
                        <th><?= $block->escapeHtml(__('Product')) ?></th>
                        <th style="width: 80px; text-align: center;"><?= $block->escapeHtml(__('Qty')) ?></th>
                        <th style="width: 120px; text-align: right;"><?= $block->escapeHtml(__('Total')) ?></th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($orderItems as $item): ?>
                    <tr>
                        <td>
                            <?php if (!empty($item['image'])): ?>
                                <img src="<?= $block->escapeUrl($item['image']) ?>"
                                     alt="<?= $block->escapeHtmlAttr($item['name'] ?? '') ?>"
                                     class="amwal-items-table__img">
                            <?php endif; ?>
                        </td>
                        <td class="amwal-items-table__name">
                            <?php if (!empty($item['url'])): ?>
                                <a href="<?= $block->escapeUrl($item['url']) ?>" target="_blank">
                                    <?= $block->escapeHtml($item['name'] ?? (string)__('N/A')) ?>
                                </a>
                            <?php else: ?>
                                <?= $block->escapeHtml($item['name'] ?? (string)__('N/A')) ?>
                            <?php endif; ?>
                        </td>
                        <td style="text-align: center;"><?= (int)($item['quantity'] ?? 0) ?></td>
                        <td style="text-align: right; font-family: monospace;"><?= $block->escapeHtml($block->formatCurrency($item['total'] ?? 0)) ?></td>
                    </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>

            <!-- Order Meta -->
            <div class="amwal-meta-row" style="padding: 12px 20px; border-top: 1px solid #f3f4f6;">
                <span>
                    <strong><?= $block->escapeHtml(__('Order Position:')) ?></strong>
                    <?= $block->escapeHtml($amwalData['order_details']['order_position'] ?? (string)__('N/A')) ?>
                </span>
                <span class="amwal-meta-row__separator"></span>
                <span>
                    <strong><?= $block->escapeHtml(__('Plugin Version:')) ?></strong>
                    <?= $block->escapeHtml($amwalData['order_details']['plugin_version'] ?? (string)__('N/A')) ?>
                    <a href="https://github.com/amwal-tech/amwal-magento/releases" target="_blank" class="amwal-link" style="margin-left: 4px;">
                        <?= $block->escapeHtml(__('Check Updates')) ?>
                    </a>
                </span>
            </div>
        </div>
    </div>
    <?php endif; ?>

    <!-- ========== SECTION: Refund Info (conditional) ========== -->
    <?php if ($block->hasRefundInfo()): ?>
    <div class="amwal-section" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <polyline points="23 4 23 10 17 10"/>
                    <path d="M20.49 15a9 9 0 11-2.12-9.36L23 10"/>
                </svg>
                <?= $block->escapeHtml(__('Refund Information')) ?>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <div class="amwal-kv-grid">
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Refunded Amount')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($block->formatCurrency($amwalData['refunded_amount'] ?? 0)) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Refundable')) ?></span>
                    <span class="amwal-kv-value">
                        <?php $isRefundable = !empty($amwalData['is_refundable']); ?>
                        <span class="amwal-badge <?= $isRefundable ? 'amwal-badge--success' : 'amwal-badge--default' ?>">
                            <?= $block->escapeHtml($isRefundable ? (string)__('Yes') : (string)__('No')) ?>
                        </span>
                    </span>
                </div>
            </div>
        </div>
    </div>
    <?php endif; ?>

    <!-- ========== SECTION: Merchant Info (collapsed by default) ========== -->
    <div class="amwal-section amwal-section--collapsed" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle>
            <h3 class="amwal-section__title">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/>
                    <polyline points="9 22 9 12 15 12 15 22"/>
                </svg>
                <?= $block->escapeHtml(__('Merchant Information')) ?>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <div class="amwal-kv-grid">
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Business Name')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['merchant_business_name'] ?? (string)__('N/A')) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('English Name')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['merchant_english_business_name'] ?? (string)__('N/A')) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Country')) ?></span>
                    <span class="amwal-kv-value"><?= $block->escapeHtml($amwalData['merchant_country_code'] ?? (string)__('N/A')) ?></span>
                </div>
                <div class="amwal-kv-item">
                    <span class="amwal-kv-label"><?= $block->escapeHtml(__('Domain')) ?></span>
                    <span class="amwal-kv-value">
                        <?php if (!empty($amwalData['merchant_domain'])): ?>
                            <a href="<?= $block->escapeUrl($amwalData['merchant_domain']) ?>" target="_blank">
                                <?= $block->escapeHtml($amwalData['merchant_domain']) ?>
                            </a>
                        <?php else: ?>
                            <?= $block->escapeHtml((string)__('N/A')) ?>
                        <?php endif; ?>
                    </span>
                </div>
            </div>
        </div>
    </div>

    <!-- ========== SECTION: Errors (conditional) ========== -->
    <?php $orderErrors = $block->getOrderErrors(); ?>
    <?php if (!empty($orderErrors)): ?>
    <div class="amwal-section" data-amwal-section>
        <div class="amwal-section__header" data-amwal-toggle style="background: #fef2f2; border-color: #fecaca;">
            <h3 class="amwal-section__title" style="color: #991b1b;">
                <svg class="amwal-section__title-icon" viewBox="0 0 24 24" fill="none" stroke="#dc2626" stroke-width="2">
                    <circle cx="12" cy="12" r="10"/>
                    <line x1="15" y1="9" x2="9" y2="15"/>
                    <line x1="9" y1="9" x2="15" y2="15"/>
                </svg>
                <?= $block->escapeHtml(__('Order Errors')) ?>
                <span class="amwal-badge amwal-badge--danger" style="font-size: 11px;"><?= count($orderErrors) ?></span>
            </h3>
            <?= $svgChevron ?>
        </div>
        <div class="amwal-section__content">
            <?php foreach ($orderErrors as $error): ?>
            <div class="amwal-error-row">
                <svg class="amwal-error-row__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/>
                    <line x1="12" y1="9" x2="12" y2="13"/>
                    <line x1="12" y1="17" x2="12.01" y2="17"/>
                </svg>
                <div class="amwal-error-row__text">
                    <?php if (!empty($error['type'])): ?>
                        <span class="amwal-error-row__type"><?= $block->escapeHtml($error['type']) ?>:</span>
                    <?php endif; ?>
                    <?= $block->escapeHtml($error['errorMsg'] ?? $error['description'] ?? (string)__('Unknown error')) ?>
                </div>
                <a href="mailto:support@amwal.tech?subject=<?= urlencode('Support Request for Order ' . ($amwalData['id'] ?? '')) ?>"
                   class="amwal-btn amwal-btn--outline amwal-btn--sm" style="flex-shrink: 0;">
                    <?= $block->escapeHtml(__('Contact Support')) ?>
                </a>
            </div>
            <?php endforeach; ?>
        </div>
    </div>
    <?php endif; ?>

</div><!-- /.amwal-tab-container -->
<?php endif; ?>

<!-- JavaScript: Collapse toggle & Update Order Status -->
<script>
require(['jquery', 'mage/translate'], function($, $t) {
    'use strict';

    // ---- Section collapse toggle ----
    $(document).on('click', '[data-amwal-toggle]', function(e) {
        e.preventDefault();
        var $section = $(this).closest('[data-amwal-section]');
        $section.toggleClass('amwal-section--collapsed');
    });

    // ---- Update Order Status ----
    <?php
    $amwalDataForJs = $block->getDecodedAmwalData();
    $jsConfig = [
        'orderId'      => $currentOrder ? $currentOrder->getIncrementId() : '',
        'amwalOrderId' => $amwalDataForJs['id'] ?? '',
        'refId'        => $amwalDataForJs['ref_id'] ?? '',
        'amwalStatus'  => $amwalDataForJs['status'] ?? '',
        'storeStatus'  => $storeStatus,
    ];
    ?>
    var config = <?= json_encode($jsConfig) ?>;

    // Bind handler if the button exists (PHP shouldShowUpdateButton() controls visibility)
    var failedStatuses = ['fail', 'failed', 'expired', 'cancelled'];
    var pendingStatuses = ['pending', 'pending_payment'];
    var shouldBind = (config.amwalStatus === 'success' && config.storeStatus !== 'processing')
        || (failedStatuses.indexOf(config.amwalStatus) !== -1 && pendingStatuses.indexOf(config.storeStatus) !== -1);

    if (shouldBind) {
        $(document).on('click', '.amwal-order-status-update', function() {
            var $btn = $(this);
            $btn.prop('disabled', true).text($t('Updating...'));

            fetch('/rest/V1/amwal/order/status', {
                method: 'POST',
                body: JSON.stringify({
                    order_id: config.orderId,
                    amwal_order_id: config.amwalOrderId,
                    ref_id: config.refId
                }),
                headers: {
                    'Content-type': 'application/json; charset=UTF-8'
                }
            })
            .then(function(response) { return response.json(); })
            .then(function(data) {
                if (data.message) {
                    alert(data.message);
                }
                location.reload();
            })
            .catch(function() {
                alert($t('An error occurred while updating the order status.'));
                $btn.prop('disabled', false).text($t('Update Order Status'));
            });
        });
    }
});
</script>
